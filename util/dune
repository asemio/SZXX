(executable
  (name cli)
  (libraries
    SZXX
    eio_main
    yojson
  )
  (preprocess (pps
    ppx_jane
  ))
  (modes exe)
  (ocamlopt_flags -O3 -unbox-closures)
)

(env
  (dev
    (flags (:standard -warn-error -A)))
)
